[phases.setup]
nixPkgs = ["php", "php81Extensions.mysqli", "php81Extensions.gd", "php81Extensions.mbstring"]

[phases.install]
cmds = ["echo 'PHP environment configured'"]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ." 